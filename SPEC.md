# LLM 可视化实验室 · 项目说明书

> 一句话定位：**一个用交互可视化讲清楚大模型底层原理的中文站点，顺带把我自己「上下文为什么会爆」的困惑解释明白。**

- 项目代号：`llm-viz-lab`
- 对外站名：**Token之庭**（HJ 已确认）
- 立项：2026-09-15

---

## 一、这个项目不做什么（边界，最重要的一节）

写死在这里。中途任何"顺便加一下"的想法，先回来看看是否违反了这些：

- ❌ **不接任何 LLM API** — 不要 key、不要联网请求、不产生费用
- ❌ **不做后端、不做数据库、不做用户登录** — 全静态
- ❌ **不做成教程网站** — 不是博客，是"能上手玩的实验场"，文字服务于交互
- ❌ **不在 v0.1 引入任何 ML 框架** — transformers.js 留给 v0.2
- ❌ **不做移动端重度适配** — 桌面优先，手机端能用就行

**判断标准**：任何新想法，如果不能回答"它让用户多懂了一个什么原理"，就砍掉。

---

## 二、技术栈（锁定，中途不换）

| 项 | 选择 | 理由 |
|---|---|---|
| 框架 | Vue 3 + TypeScript + Vite | 已有能力圈。新知识预算留给 LLM 原理，不留给框架 |
| 路由 | Vue Router | 每个实验一个独立页面 |
| 状态 | 不引入 Pinia | 各页面互不依赖，页面内 `ref` 足够 |
| UI 库 | **不用** | 组件库会绑架视觉风格，这套二次元风格需要自己写 |
| 图表 | **手写 SVG** | 可视化的形状就是教学内容本身，用库反而隔了一层 |
| 分词 | v0.1 自己实现 BPE（教学用）+ `gpt-tokenizer`（真实词表用） | 自己实现才是学习，真实词表才有说服力 |
| 部署 | **WorkBuddy 内置发布**（已上线）<br>备用：GitHub Pages / Vercel | 零成本。发布的是 `dist/` 纯静态产物 |

**v0.1 唯一允许的运行时依赖**：`gpt-tokenizer`。其余一律手写。

> 补充：`@types/node` 是开发依赖（只在写配置和构建时用，不进产物），不受上面这条限制。
> 这条约束针对的是**运行时依赖** —— 进到你代码 import 里的东西才需要克制。

---

## 三、架构（方块 = 有什么，连线 = 传什么）

```
你的浏览器
    │  输入地址
    ▼
Vue Router ── 一个地址对应一个实验页面，自身不做计算
    │            │            │
    ▼            ▼            ▼
① 分词实验室   ② 上下文可视化   ③ 成本计算器
文本→token块   滑杆→三条曲线    用量→金额+构成
    │            │            │
    └────────────┼────────────┘
                 ▼  （虚线：只读取，不依赖）
        共享层：设计变量 / 水晶卡片组件 / 静态参数表
```

**关键性质**：`labs/` 下每个目录自给自足，删掉任意一个，其余照常运行。

**这张图上没有技术栈。** Vue3、Vite、gpt-tokenizer 是"用什么材料盖"，不是"房子长什么样"。
技术选型变了不影响架构；架构变了才需要回来改这一节。

---

## 四、v0.1 模块清单（三个，纯本地计算）

### 模块 1 · BPE 分词实验室

**讲清楚什么**：模型看到的不是字，是 token。以及为什么中文比英文"贵"。

**交互**
- 左：输入框，可粘贴任意文本，或选用内置示例（中 / 英 / 代码 / emoji 各一）
- 中：分词结果，每个 token 一块彩色标签，悬停显示 token id 与字节
- 右：统计面板 —— 字符数、token 数、压缩比（字符 / token）
- **对比模式**：同一段中英文并列，直观看出压缩比差异

**数据来源**：`gpt-tokenizer`（cl100k_base 词表）

**进阶区块 · BPE 训练过程**（M2，已完成）：三个预设语料 + 自定义输入，
「播放 / 上一步 / 下一步 / 重置」单步回放合并过程，实时显示词的分裂演变与词表增长。
**自己实现（`bpe.ts`），不依赖任何库** —— 这是整个项目里学习价值最高的一块。

**验收标准**：粘贴一段中文 + 一段英文，能一眼看出中文 token 数明显更多；BPE 训练过程能单步播放。

### M2 的实现要点（写给未来的自己看）

- **词表是累积的**：每步往 `Set` 里加新符号，而不是重新统计「当前出现的符号」。
  后者会让词表**先增后减**（某个字符暂时不再单独出现就消失了），那是错的 —— 真实 BPE 学到的
  合并规则会永久保留。这个 bug 第一版就犯了，验证数据时发现的
- **词尾标记 `</w>`**：加在每���词末尾防止跨词合并，UI 上显示成 `⏎`
- **平局按字典序取**：多对频次相同时取字典序小的，保证结果可复现
- **语料切分**：`/[A-Za-z0-9]+|[^\sA-Za-z0-9]/g`，中英混排都能合理工作

**已验证的经典案例**：`hug hug hug pug pug pun pun pun bun bun hugging`
10 步后词表从 8 涨到 18，`hug` / `pun` / `pug` / `bun` 各自收敛成单个 token。

---

### 模块 2 · 上下文与 KV Cache 可视化

**讲清楚什么**：我的上下文为什么会越聊越爆，代价到底是怎么涨的。

**交互**
- 「模拟对话」滑杆：拖动对话轮数 / 每轮长度
- 实时显示三条曲线：
  - **token 累积**（线性增长）
  - **attention 计算量**（随 n² 增长）
  - **KV cache 显存占用**（随 n 增长，标注每层每 token 的开销）
- 模型选择器（预设几组参数：层数、hidden size、精度 fp16 / int8）
- **关键标注**：标出"超过这个长度就开始分页 / 截断"的位置

**数据来源**：硬编码的模型参数表（自己整理，见 `contextMath.ts`），纯公式计算

**三个公式（写在 `contextMath.ts` 顶部注释里）**
1. `KV cache 字节 = 2 × layers × kvHeads × headDim × seqLen × bytesPerParam`（×2 是 K 和 V 各一份）
2. `Attention FLOPs = 4 × seqLen² × hidden × layers`（4 = QKᵀ 与 AV 两个矩阵乘各 2 FLOPs）
3. `上下文长度 = 轮数 × 每轮 token 数`

**已核对的基准值**（fp16 满窗）：LLaMA-3 8B @ 8K = **1.00 GB**；
Qwen2.5-7B @ 32K = 1.75 GB；LLaMA-3 70B @ 131K = **40 GB**；GPT-3 175B @ 2K = 9 GB（无 GQA 所以很贵）。
这几个数字与业界公开共识一致，是本站可信度的基础 —— **改参数后要重新核对**。

**验收标准**：拖动滑杆，能清楚看到平方项那条线如何迅速甩开其他两条。

---

### 模块 3 · 上下文预算与成本计算器

**讲清楚什么**：一次对话到底花多少钱，我的 token 都花在哪了。

**交互**
- 输入：系统提示词长度、历史轮数、单次输出长度、日均调用次数
- 输出：单次成本、月成本、**成本构成图**（输入 / 输出 / 重复的历史上下文）
- 「优化建议」区块：基于输入数据给出可执行的降本建议

**数据来源**：静态价格表 `labs/cost/pricing.ts`（HJ 拍板**不做运行时抓取**）

### 价格数据的方案决策（HJ 2026-09-15 定）

- **方案：静态表 + 手动更新**，不做页面运行时抓取，也不做 CI 定时抓取
- **数据源：OpenRouter 公开接口** `https://openrouter.ai/api/v1/models`（返回结构化 JSON，445 个模型）
- **为什么不实时抓**：CORS 只在浏览器里有约束，构建脚本本来没有这个问题 ——
  但运行时抓取会打破「不接 API / 纯静态」的核心边界，且网络异常时整页不可用
- **自我保护**：`pricing.ts` 里的 `FETCHED_AT` 驱动页面上的**过期告警**，
  超过 60 天会显示醒目提示并指明要改哪个文件。宁可承认数据可能旧，也不假装实时
- **口径声明**：OpenRouter 报价与厂商直连价有出入（如 GPT-5.6 Sol 官方 $4、OpenRouter $2），
  页面底部明确标注，不冒充官方价
- **更新步骤**写在 `pricing.ts` 顶部注释里：拉接口 → 改数字 → 改 `FETCHED_AT` → build

**验收标准**：算出来的数字能对得上真实账单量级；建议是具体的，不是"请注意节约"；
勾选「启用 prompt 缓存」后能看出明确的省钱比例（多数模型缓存价是输入价的 1/10）。

---

## 五、v0.2 预留（现在不碰，只记录）

- 引入 `transformers.js` + GPT-2（浏览器端推理）
- 采样实验场：真实 logits，调 temperature / top-p 看分布变形
- Attention 热力图：真实权重
- Embedding 投影：token 在向量空间里的分布

**进入条件**：v0.1 上线，且我自己真的在用。

---

## 六、目录结构

```
02_llm-viz-lab/
├── SPEC.md              本文件 · 描述"它是什么样"，变化慢
├── NOW.md               当前做到哪了 · 变化快，≤20 行
├── README.md            对外说明 · v0.1 上线前才写
├── index.html
└── src/
    ├── main.ts
    ├── App.vue
    ├── router.ts
    ├── styles/
    │   ├── tokens.css   设计变量：颜色/间距/字体（唯一改风格的地方）
    │   └── base.css
    ├── components/      共享 UI：CrystalCard / PetalDivider / LabSlider
    ├── views/           页面外壳：HomeView
    └── labs/            三个实验模块，各自自包含
        ├── tokenizer/
        ├── context/
        └── cost/
```

---

## 七、视觉风格 · 爱莉希雅调性

**不放角色立绘**（版权风险 + 抢注意力）。用配色、质感、几何元素营造氛围。

**调研依据**：爱莉希雅官方设定 —— 主色粉（微卷粉长发）+ 白（英桀制服），
点缀紫（制服纹饰）与淡金（勾线）；元素语言为**水晶、花芽、翅膀铃铛、头纱、蝴蝶结、菱形格纹缎面**，
关键词「绽放、新生、爱与美」。

| Token | 值 | 用途 |
|---|---|---|
| `--sakura` 樱粉 | `#E8749B` | 主强调色、可交互元素 |
| `--sakura-light` / `--sakura-deep` | `#FFB3CE` / `#D94F80` | 渐变两端、强调文字 |
| `--pearl` 珍珠白 | `#FFFAFC` | 卡片背景 |
| `--lilac` 淡紫 | `#C9A8E0` | 副色调、页脚与次级装饰 |
| `--gold` / `--gold-light` | `#D4AF6A` / `#F2DFB0` | 点缀、星芒、数值高亮 |
| `--ink` 墨紫 | `#3B2A35` | 正文文字 |

**质感做法**（不用图片，纯 CSS / SVG）
- **背景**：粉→淡紫→浅金三段渐变，叠两团柔光 + 一层极淡菱形格纹（取自角色披风内衬）
- **卡片**：玻璃拟态（`backdrop-filter`） + 双层描边（外粉内白） + 内发光 + 顶部高光条
- **标题**：粉紫金渐变文字 + 白色描边（`.title-anime`）
- **圆角**：20px，柔和感

**新增的装饰组件**

| 组件 | 作用 |
|---|---|
| `PetalField.vue` | 14 片飘落花瓣 + 18 个浮动光点，纯 CSS，固定伪随机不跳变 |
| `HeroOrnament.vue` | 花环 + 光翼 + 水晶 + 蝴蝶结的半透明 SVG 插图 |
| `OrnamentDivider.vue` | 水晶花 + 铃铛点的装饰性分隔线 |

**红线**
- **不使用任何角色立绘 / 官方素材** —— 版权风险，且会抢走数据本身的注意力。角色氛围一律用几何图形营造
- 装饰元素透明度 ≤ 0.5（插图 ≤ 0.22），绝不能盖过数据本身
- 所有动画都走 `prefers-reduced-motion` 降级
- 好看不能牺牲可读性

---

## 八、里程碑

| 阶段 | 内容 | 状态 |
|---|---|---|
| M0 | 脚手架 + 设计 token + 装饰组件 + 首页 + 二次元视觉 | ✅ 已验收 |
| M1 | 模块 1 · 分词对比部分 | ✅ 已验收（含 P2-2 长文实测通过） |
| M2 | 模块 1 的 BPE 训练过程（最难也最值钱） | ✅ 已完成，待验收 |
| M3 | 模块 2 上下文可视化 | ✅ 已完成，待验收 |
| M4 | 模块 3 成本计算器 | ✅ 已完成，待验收 |
| M5 | README + 部署 | ✅ 已完成 |

**每个里程碑结束都要能跑、能看、能演示。** 不允许出现"三个模块同时在写但都不能跑"的状态。

---

## 八·一、M1 任务清单（一次只做一件，做完一件验收一件）

### 风格与布局锁定 —— M1 全部任务的前置约束

视觉已验收，以下四条在 M1 期间视为不可协商：

1. **只用 `tokens.css` 里已有的变量**。需要新颜色，先加进 `tokens.css`，不允许在组件里写死色值
2. **容器一律复用 `CrystalCard`**，不自建卡片样式；装饰复用 `OrnamentDivider`
3. **不引入 UI 库、图表库**。token 块、对比条全部手写
4. **沿用桌面优先 + `≤720px` 单列断点**，与首页保持一致

### 任务表

| 优先级 | 任务 | 涉及文件 | 完成标准 | 验收方式 |
|---|---|---|---|---|
| **P0-1**<br>✅ 已验收 | 引入 `gpt-tokenizer` 并封装 | `package.json`（改）<br>`labs/tokenizer/tokenizer.ts`（新） | 暴露「异步」`encode(text)` → `{ ids, tokens, count, chars, ratio, lossless }`；另导出 `loadTokenizer()` 供后台预加载、`isLoaded()` 查状态；计算部分是纯函数 | `Hello world` 得 2 tokens（13225, 2375）、`Hello, world!` 得 4 tokens（13225, 11, 2375, 0），与 OpenAI 官方 tokenizer 页面一致；五组样例全部 `lossless = true` |

> **词表加载方案（HJ 选定：首屏优先 + 后台加载）**
> cl100k 词表约 1MB（gzip），不进主包。`tokenizer.ts` 内用 `import('gpt-tokenizer')` 动态引入，
> 页面挂载时调 `loadTokenizer()` 异步拉取，**先出骨架屏，词表到了再填数据**。
> 后续可在首页卡片 hover 时提前 `loadTokenizer()` 做 prefetch（待办，非当前阶段）。
| **P0-2**<br>✅ 已完成 | 分词页骨架与路由 | `router.ts`（改）<br>`labs/tokenizer/TokenizerLab.vue`（新） | 面包屑 + 页头 + `OrnamentDivider` + `300px / 1fr` 双列；≤860px 转单列；容器一律 `CrystalCard` | 视觉与首页同源；面包屑返回正常；窗口拖窄到 860px 以下自动单列且不横向滚动 |
| **P1-1**<br>✅ 已完成 | 输入区 + 四个预设 | `labs/tokenizer/TextInput.vue`（新）<br>`TokenizerLab.vue`（改） | `v-model` 双向绑定；四预设一键填入且当前项高亮；输入防抖 220ms 后重算；实时字符数、清空按钮 | 逐一点击四个预设都正确填入并立即出结果；输入变化右侧 Token 流跟着更新 |
| **P1-2**<br>✅ 已完成 | token 块渲染 | `labs/tokenizer/TokenChips.vue`（新）<br>`TokenizerLab.vue`（改） | 悬停显示 `#id · 字符数 · 含不可见字符`；空格 `·` / 换行 `↵` / 制表 `⇥` / 空片段 `∅` 全部可视化；相邻块樱粉与淡紫交替底色；单页渲染上限 1200，超出提示未渲染数量 | 用「代码」预设（含换行）验收：↵ 的个数等于原文换行数；肉眼顺着读一遍就是原文 |
| **P1-3**<br>✅ 已完成 | 统计面板 | `labs/tokenizer/StatsPanel.vue`（新） | 字符 / token / 压缩比三个数字 + 0~8 量程条形图 + 定性判断（高效 / 中等 / 偏贵 / 很贵） | 手工核对：压缩比 = 字符数 ÷ token 数；条形长度 = 压缩比 ÷ 8 × 100% |
| **P1-4**<br>✅ 已完成 | 中英文对比模式 | `labs/tokenizer/CompareMode.vue`（新） | 三组同语义中英对照，各显示 token 数与压缩比，樱紫双条形并列；底部给出「贵 N 倍」 | 中文压缩比明显低于英文；「贵 N 倍」= 英文压缩比 ÷ 中文压缩比，手工核对一致 |
| **P2-1**<br>✅ 已提前完成 | 首页激活入口 | `views/HomeView.vue`（改） | tokenizer 卡 `ready: true`；未开放卡片加 `.is-off` 降权且不做 hover 位移；hover/focus 时 `loadTokenizer()` 预热 | 首页点第一张卡进得去；另两张点击不跳转；hover 后点进去不再出现骨架屏 |
| **P2-2**<br>✅ 已验收 | 边界与性能 | 上述文件 | 空输入有友好提示；5000 字输入 2 秒内出结果不卡死 | HJ 实测长文通过 |

### 明确不在 M1 范围内

- ❌ BPE 训练过程可视化 → **M2**，单独验收
- ❌ 模块 2、模块 3 → M3 / M4
- ❌ 任何新配色、新布局体系、新组件库
- ❌ 移动端深度适配

### 单文件约束

每个新文件 ≤ 200 行。超出就按「输入 / 计算 / 渲染」拆 —— 这也正好对应 M0 架构图里每个 lab 的三段结构。

---

## 九、防烂尾约定

1. **架构和数据模型我自己定** —— AI 只写实现，不让它决定模块边界
2. **单个文件 ≤ 200 行**，超了就拆
3. **说不出代码在干什么就删掉重写**，不硬修
4. **看不懂的库不用** —— 宁可手写 50 行，也不引一个读不透的依赖
5. **想加功能先回来改这份 SPEC**，而不是直接改代码
6. **AI 推荐技术选型时，答不上"它是干嘛的"就不许说 ok**

---

## 十、文档纪律（防止文档通胀）

**整个项目只允许存在三份 md**：`SPEC.md`、`NOW.md`、`README.md`（上线前才建）。

- **不允许新建第四份。** 有新内容就改 SPEC 对应章节，而不是新开一份
- **每份文档顶部必须能一句话说清"它回答什么问题"**，说不出来就说明它不该存在
- **SPEC 写"它现在是什么样"，不写"我打算做什么"** —— 计划会过时，描述不会
- **SPEC 与代码冲突时**：先判断是 SPEC 想得太美还是实现跑偏了。多数时候是前者，改 SPEC 是允许的，不算失败
- **文档是给我自己看的，不是喂给 AI 的。** 每次让 AI 干活，只给它相关文件 + 当前这一步的任务，不要整份文档丢过去

---

## 十一、待确认

- [x] 站名 —— 已确认 **Token之庭**
- [x] 词表加载方案 —— 已确认「首屏优先 + 后台加载」
- [x] 模块顺序 —— 已按 M1→M4 顺序走完
- [x] 价格数据来源 —— 已定 OpenRouter 公开接口 + 静态表手动更新
- [ ] M5 是否要真的部署到 GitHub Pages
