# NOW · 当前进度

> 只回答一个问题：**现在做到哪了，下一步干什么。**
> 有新进展就改这个文件，不要新建别的文档。

## 当前阶段

# 🎉 v0.1 全部完成并已上线

**M0 ✅ → M1 ✅ → M2 ✅ → M3 ✅ → M4 ✅ → M5 ✅**

**线上地址：https://token-garden.app.workbuddy.host/**
（发布的 `dist/` 目录，纯静态；管理入口在 WorkBuddy「设置—数据管理—应用」）

**更新线上内容的流程**：改代码 → `npm run build` → 重新发布 `dist` 目录

## 本轮确认

- [x] **站名定为 `Token之庭`**（已改 `App.vue`、`index.html`、SPEC）
- [x] **词表方案 3「首屏优先 + 后台加载」已实施**

## 已完成

- [x] P0-1：`gpt-tokenizer` 封装，`encode()` 改为异步 + 导出 `loadTokenizer()` / `isLoaded()`
- [x] 页面挂载后异步拉词表，**先出骨架屏**，到了再填数据
- [x] **P0-2：分词页正式骨架** —— 面包屑、页头、OrnamentDivider、`300px + 1fr` 双列（≤860px 转单列）
- [x] **修复：首页点不进分词页** —— 根因是三张卡 `ready` 全为 `false`，
      `:to` 被算成 `'/'` 等于原地返回。已把 tokenizer 置 `ready: true`，并给未开放卡片加 `.is-off` 降权样式
- [x] **首页 hover prefetch** —— `@mouseenter` / `@focus` 触发 `loadTokenizer()`，幂等只加载一次
- [x] **P1-1：输入区 + 四预设** —— `TextInput.vue`；v-model 绑定、当前预设高亮、实时字符数、清空、防抖 220ms
- [x] **P1-2：Token 块正式渲染** —— `TokenChips.vue`
  - 悬停显示 `#id · 字符数 · 含不可见字符`
  - 不可见字符可视化：空格 `·` / 换行 `↵` / 制表 `⇥` / 空片段 `∅`，页脚有图例
  - 相邻块樱粉 ↔ 淡紫交替底色，边界一眼可辨；含不可见字符的块用淡金高亮
  - 单页渲染上限 1200（长文不卡死，超出显示未渲染数量）
- [x] **P1-3：统计面板** —— `StatsPanel.vue`；字符 / token / 压缩比 + 0~8 量程条形 + 定性判断
- [x] **P1-4：中英对比** —— `CompareMode.vue`；三组同语义中英对照 + 双条形 + 「贵 N 倍」
      （新增 `--tone-good/mid/bad` 三个语义色进 `tokens.css`）
- [x] **P2-2 边界与性能 —— HJ 实测长文通过**
- [x] **M2：BPE 训练过程可视化**
  - `bpe.ts` —— 算法自己实现，零依赖；记录每一步的 pair / 频次 / 快照 / 词表
  - `BpeTrainer.vue` —— 三预设语料 + 自定义、合并步数滑块、播放/单步/回退/重置、
    词的分裂演变（本步新符号高亮）、词表增长、`⏎` 词尾标记图例
  - 已验证：`hug hug hug pug pug pun pun pun bun bun hugging` 十步后词表 8→18，
    `hug`/`pun`/`pug`/`bun` 各自收敛成单 token
- [x] **M3：上下文与 KV Cache 可视化**（新页面 `/#/labs/context`）
  - `contextMath.ts` —— 三个标准公式 + 4 个真实模型参数 + 3 种精度
  - `ContextChart.vue` —— 手写 SVG 折线图（不引图表库），带参考线
  - `ContextLab.vue` —— 参数区 + 三数值卡 + 24GB 显卡显存条 + 双曲线对比 + 超限告警
  - 首页第二张卡已激活
- [x] **M4：成本计算器**（新页面 `/#/labs/cost`）
  - `pricing.ts` —— 8 个主流模型静态价格表（OpenAI / Anthropic / Google / DeepSeek / Qwen），
    带 `FETCHED_AT` 驱动过期告警，顶部注释写了手动更新步骤
  - `costMath.ts` —— 成本构成拆成「重复历史 / system 重发 / 本轮新输入 / 输出」四项 + 三条优化建议
  - `CostLab.vue` —— 参数区（含缓存开关）+ 三档成本（单次/日/月）+ 构成条形 + 建议区 + 来源声明
  - 首页第三张卡已激活
- [x] `npm run build` 通过，vue-tsc 无错（103 模块）

## M4 的价格数据（2026-09-15 抓自 OpenRouter）

| 厂商 | 模型 | 输入 /1M | 输出 /1M | 缓存 /1M | 缓存倍数 |
|---|---|---|---|---|---|
| OpenAI | GPT-6 Astra | $10 | $50 | $1 | 10× |
| OpenAI | GPT-5.6 Sol | $2 | $10 | $0.20 | 10× |
| OpenAI | GPT-5.6 Luna | $0.20 | $1.20 | $0.02 | 10× |
| Anthropic | Claude Opus 5 | $5 | $25 | $0.50 | 10× |
| Anthropic | Claude Sonnet 5 | $2 | $10 | $0.20 | 10× |
| Google | Gemini 3.5 Flash | $1.50 | $9 | $0.15 | 10× |
| DeepSeek | V4 Pro | $1.60 | $3.20 | $0.135 | ≈12× |
| Qwen | Qwen3.8 Max | $2 | $6 | $0.25 | 8× |

**缓存价普遍是输入价的 1/10** —— 这是成本页最有教学价值的一点。

## M3 核对过的基准值（fp16 满窗）

| 模型 | 窗口 | KV cache | 备注 |
|---|---|---|---|
| LLaMA-3 8B | 8K | **1.00 GB** | 业界常引用的数字，正好对上 |
| Qwen2.5-7B | 32K | 1.75 GB | KV head 只有 4 个，很省 |
| LLaMA-3 70B | 131K | **40 GB** | 层数 80，涨得飞快 |
| GPT-3 175B | 2K | 9.00 GB | MHA 无 GQA，窗口小也贵 |

**演化实测（8B，每轮 512 tok）**：轮 1 → 16，显存 0.063→1.00 GB（×16），
计算量 0.14→35.18 TFLOPs（**×251**）。平方项原形毕露。

## M2 里修掉的一个准确性 bug（值得记住）

第一版统计「当前文本里出现的符号」当词表，导致**词表先增后减**（step 6 是 14，step 8 掉回 9）。
真实 BPE 的词表是**累积的** —— 学到的合并规则永久保留，某个字符暂时不出现不代表它消失了。
改成 `Set` 累加后，词表单调 8→18。这个错如果没跑数据验证，光看页面是发现不了的。

## P1-4 实测数据（这是整个模块最有价值的部分）

| 对照 | 中文 | token | 压缩比 | 英文 | token | 压缩比 | 中文贵 |
|---|---|---|---|---|---|---|---|
| 日常 | 18 字 | 13 | **1.38** | 60 字 | 16 | 3.75 | **2.71 倍** |
| 技术 | 18 字 | 11 | **1.64** | 62 字 | 10 | 6.20 | **3.79 倍** |
| 描写 | 23 字 | **28** | **0.82** | 92 字 | 19 | 4.84 | **3.89 倍** |

**最值得看的是「描写」那组：23 个字切出了 28 个 token —— token 数比字数还多，压缩比跌到 0.82。**
原因不是"中文天生贵"，而是这类文学化用字在 cl100k 词表里覆盖差，会被拆成 UTF-8 字节级别。
换句话说：**中文贵多少，跟你的用词常不常见直接相关**，不只是语言特性。

## P1-2 怎么验收

点右上角「代码」预设（它带换行），然后顺着 token 块读一遍：

- `↵` 的个数应该正好等于原文的换行数（代码预设是 2 个）
- `·` 就是原文里的空格
- 把每块显示的符号**翻译回去**连起来读，就是原文本身
- 鼠标停在任意一块上，会显示它的 token id 和字符数

这一步的意义在于：**看不见的字符不等于不存在**。以前很多人以为空格被"丢掉"了，
其实它好好地待在某个 token 里，只是渲染时没显示出来。

## 四个预设的实际分词结果（P1-1 验收数据）

| 预设 | 字符 | token 数 | 压缩比 | 无损 |
|---|---|---|---|---|
| 中文「人工智能正在改变我们编写软件的方式。」 | 18 | **11** | **1.64** | ✓ |
| English「Artificial intelligence is changing the way we write software.」 | 62 | **10** | **6.20** | ✓ |
| 代码 | 51 | 15 | 3.40 | ✓ |
| Emoji `🌸✨🎉💖🎀` | 9 | 9 | 1.00 | ✓ |

**这组数就是整个实验要讲的核心**：中英语义几乎相同、token 数也几乎相同（11 vs 10），
但字符数差 3.4 倍 —— 英文每个 token 承载 6.20 个字符，中文只有 1.64 个，**中文贵约 3.8 倍**。
Emoji 是 1 字符 = 1 token，压缩比正好 1.00。

## 拆分效果（方案 3 的实际收益）

| chunk | 改造前 | 改造后 |
|---|---|---|
| `TokenizerLab.js` | 2,038 kB（gzip 1,025） | **2.38 kB**（gzip 1.34） |
| 词表 `main.js` | 混在上者包内 | 独立 2,037 kB，按需异步加载 |
| 主包 `index.js` | 99 kB | 99 kB（本来就不含词表） |

进入分词页先渲染 2.38 kB 的页面代码，1MB 词表后台拉取，期间显示樱花渐变骨架条。

## P0-1 自检结果（已验收）

| 输入 | 字符 | token 数 | 压缩比 | 无损 | ids |
|---|---|---|---|---|---|
| `Hello world` | 11 | **2** | 5.50 | ✓ | 13225, 2375 |
| `Hello, world!` | 13 | **4** | 3.25 | ✓ | 13225, 11, 2375, 0 |
| `你好，世界` | 5 | **3** | 1.67 | ✓ | 177519, 979, 28428 |
| `こんにちは` | 5 | **1** | 5.00 | ✓ | 95839 |
| `🎉🎊` | 4 | **4** | 1.00 | ✓ | 71344, 231, 71344, 232 |

全部 `lossless = ✓` —— P1-2 的验收前提成立。

## 下一步

**下一步：先自己用几天，再决定 v0.2**

建议 HJ 做的事：
1. 把线上链接在手机上打开一次，看窄屏下三个实验是否都能用
2. 把三个实验都真用一遍，记下哪里"讲得不清楚"—— 这是 v0.1 之后最值得改的东西
3. 特别试试分词页的 BPE 训练器，它是整站最复杂的一块

**v0.2 候选（SPEC 第五节已记录，但先不做）**
- `transformers.js` + GPT-2：真实 logits 做采样实验、真实 attention 权重做热力图
- 采样参数实验场（temperature / top-p 对分布的影响）
- Embedding 投影

**进入条件**：v0.1 上线且我自己真的在用。

## 已知问题 / 待定

- 首页卡片 hover prefetch 已实现（不再是待办）
- M1 目前只开放 tokenizer，另两张卡降权显示、不可点
- M2（BPE 训练过程）单独排期

## 本地怎么跑

```bash
npm run dev      # 开发
npm run build    # 类型检查 + 构建
```
